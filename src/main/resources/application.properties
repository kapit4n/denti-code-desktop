# Denti-Code Desktop application configuration.
# These can be overridden by JVM system properties: -Dapp.dataDir=...

app.name=Denti-Code Desktop
app.version=0.1.0
# Data directory is resolved at runtime; default is <user.home>/.denticode-desktop
app.dataDir=
app.locale=en
app.demoMode=true
